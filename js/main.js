document.addEventListener("DOMContentLoaded",()=>{

    initializeMusic();

    initializeTimeline();

    initializeReasons();

    initializeNavigation();

    initializeGallery();

    initializeLetter();

    console.log("Application Started ❤️");

});